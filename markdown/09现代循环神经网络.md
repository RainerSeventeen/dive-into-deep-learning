# 现代循环神经网络

## Seq2Seq

最早用来机器学习中的语言翻译，，是一个 Encoder 和 Decoder 的架构

编码器是一个 RNN（可以双向，因为基本都能看到完整的句子）， 解码器是另一个 RNN 的架构（没法双向，应该要学会预测）

把输入全部给 Enc，然后把隐藏的状态给 Dec，反复循环输出直到输出 `<eos>`句子结束符号

- 训练：在训练的时候 Dec 的输入是一个正确的句子，移除 eos，推理时候加入 eos 

- 推理：怎么衡量两个不同长度的句子之间的好坏？常用的方法是 BLEU



